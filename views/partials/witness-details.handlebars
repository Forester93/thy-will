<button class="w-100 updateBtn witnessBtn" data-toggle="modal" data-target="#witnessModal" data='{{{unobjectify this}}}'> 
    <span>{{name}}</span>
    <i class="fas fa-times witnessDelete" data='{{{unobjectify this}}}'></i>
</button>